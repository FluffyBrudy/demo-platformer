from pathlib import Path

ROOT_PATH = Path(__file__).parent.parent

ASSETS_PATH = ROOT_PATH / "assets" / "data"

MAPS_PATH = ROOT_PATH / "data" / "maps"
CHARACTER_COLLISION_PATH = ROOT_PATH / "data" / "character_collision"
TILESET_COLLISION_PATH = ROOT_PATH / "data" / "collision"
ANIMATION_PATH = ROOT_PATH / "data" / "animations"
